import { readFileSync } from "node:fs";import { describe,expect,it } from "vitest";
describe("Task 18 architecture",()=>{const publicPage=readFileSync("src/app/blog/[slug]/page.tsx","utf8"),preview=readFileSync("src/app/admin/posts/[id]/preview/page.tsx","utf8"),actions=readFileSync("src/app/admin/posts/actions.ts","utf8"),sitemap=readFileSync("src/app/sitemap.ts","utf8"),migration=readFileSync("drizzle/0016_seo_content_cms.sql","utf8");it("keeps public queries published-only and preview admin-only",()=>{expect(readFileSync("src/lib/blog/service.ts","utf8")).toContain('eq(contentPosts.status,"PUBLISHED")');expect(preview).toContain('requireAdmin("CONTENT_READ")');expect(preview).toContain("index:false");});it("ships SEO metadata, canonical, OG and BlogPosting JSON-LD",()=>{expect(publicPage).toContain("generateMetadata");expect(publicPage).toContain("alternates:{canonical}");expect(publicPage).toContain("openGraph");expect(publicPage).toContain('"@type":"BlogPosting"');});it("revalidates public routes and dynamic sitemap",()=>{expect(actions).toContain('revalidatePath("/sitemap.xml")');expect(sitemap).toContain("publishedSitemapRows");});it("stores provider-independent media IDs",()=>{expect(migration).toContain('"cover_media_id" uuid');expect(migration).not.toMatch(/r2.*url|cloudflare.*url/i);});});
