import { afterEach,describe,expect,it,vi } from "vitest";import { getPaymentCatalog,resolveProduct } from "./catalog";
afterEach(()=>vi.unstubAllEnvs());
describe("payment catalog security",()=>{it("keeps an unconfigured product unavailable",()=>{vi.stubEnv("PREMIUM_30_PRICE_VND","");expect(getPaymentCatalog()[0].amountVnd).toBeNull();expect(()=>resolveProduct("PREMIUM_30_DAYS")).toThrow("PRODUCT_NOT_AVAILABLE")});it("accepts only positive integer VND with a ready provider",()=>{vi.stubEnv("PAYMENT_PROVIDER","FAKE");vi.stubEnv("NODE_ENV","test");vi.stubEnv("PREMIUM_30_PRICE_VND","99000");expect(resolveProduct("PREMIUM_30_DAYS")).toMatchObject({days:30,amountVnd:99000,currency:"VND",purchasable:true});vi.stubEnv("PREMIUM_30_PRICE_VND","1.5");expect(getPaymentCatalog()[0].amountVnd).toBeNull()});it("disables purchase when credentials are incomplete",()=>{vi.stubEnv("PAYMENT_PROVIDER","PAYOS");vi.stubEnv("PREMIUM_30_PRICE_VND","99000");vi.stubEnv("PAYOS_CLIENT_ID","configured");expect(getPaymentCatalog()[0].purchasable).toBe(false);expect(()=>resolveProduct("PREMIUM_30_DAYS")).toThrow("PRODUCT_NOT_AVAILABLE")});it("has exactly the supported durations",()=>expect(getPaymentCatalog().map(p=>p.days)).toEqual([30,90,365]))});
