import Link from "next/link";import { LearnerNav } from "@/components/learner-nav";import { requireUser } from "@/lib/auth/session";import { getEffectivePlan } from "@/lib/entitlements/service";import { getPreferences } from "@/lib/i18n/get-translations";import { getPaymentCatalog } from "@/lib/payments/catalog";import { getPremiumExpiry,listUserOrders } from "@/lib/payments/service";import { createCheckoutAction } from "./actions";
const money=(value:number)=>new Intl.NumberFormat("vi-VN").format(value)+" ₫";
export default async function BillingPage({searchParams}:{searchParams:Promise<{error?:string}>}){const user=await requireUser();const[prefs,plan,expiry,orders]=await Promise.all([getPreferences(user.id),getEffectivePlan(user.id),getPremiumExpiry(user.id),listUserOrders(user.id)]);const vi=prefs.interfaceLanguage==="vi",error=(await searchParams).error;return <main className="min-h-screen bg-slate-50 px-4 py-6 text-slate-900 sm:px-6"><div className="mx-auto max-w-6xl"><LearnerNav locale={prefs.interfaceLanguage}/><div className="mt-8 grid gap-6"><section className="rounded-3xl border bg-white p-6"><h1 className="text-3xl font-black">{vi?"Thanh toán":"Billing"}</h1><p className="mt-3 font-bold">{vi?"Gói hiện tại":"Current plan"}: {plan}</p>{expiry?<p className="mt-1 text-slate-600">{vi?"Premium đến":"Premium until"}: {expiry.toLocaleString(vi?"vi-VN":"en-US")}</p>:null}{error?<p className="mt-4 rounded-xl bg-amber-50 p-3" role="alert">{vi?"Thanh toán hiện không khả dụng.":"Payment is unavailable."}</p>:null}</section><section className="grid gap-4 sm:grid-cols-3">{getPaymentCatalog().map(p=><article className="rounded-2xl border bg-white p-5" key={p.key}><h2 className="text-xl font-black">{p.days} {vi?"ngày":"days"}</h2><p className="mt-2 font-bold">{p.amountVnd?money(p.amountVnd):(vi?"Chưa mở bán":"Not available")}</p>{p.purchasable?<form action={createCheckoutAction} className="mt-4"><input name="productKey" type="hidden" value={p.key}/><button className="min-h-11 w-full rounded-xl bg-teal-700 px-4 font-bold text-white focus:outline-2">{vi?"Thanh toán ngay":"Pay now"}</button></form>:<p className="mt-4 text-sm font-semibold text-slate-500">{vi?"Chưa mở bán":"Payment unavailable"}</p>}</article>)}</section><section className="rounded-3xl border bg-white p-6"><h2 className="text-xl font-black">{vi?"Lịch sử thanh toán":"Billing history"}</h2><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[40rem] text-left text-sm"><thead><tr><th className="p-3">{vi?"Sản phẩm":"Product"}</th><th className="p-3">{vi?"Số tiền":"Amount"}</th><th className="p-3">{vi?"Trạng thái":"Status"}</th><th className="p-3">{vi?"Tạo lúc":"Created"}</th><th className="p-3">{vi?"Thanh toán lúc":"Paid"}</th></tr></thead><tbody>{orders.map(o=><tr className="border-t" key={o.id}><td className="p-3"><Link className="font-bold text-teal-700" href={`/billing/return?order=${o.id}`}>{o.productKey}</Link></td><td className="p-3">{money(o.amount)}</td><td className="p-3">{o.status}</td><td className="p-3">{o.createdAt.toLocaleString()}</td><td className="p-3">{o.paidAt?.toLocaleString()??"—"}</td></tr>)}</tbody></table></div></section></div></div></main>}
