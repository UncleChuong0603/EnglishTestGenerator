import Link from "next/link";import { AdminNav } from "@/components/admin/admin-nav";import { requireAdmin } from "@/lib/admin/authorization";import { listAdminPosts } from "@/lib/blog/service";import { getPreferences } from "@/lib/i18n/get-translations";
export default async function PostsPage(){const actor=await requireAdmin("CONTENT_READ"),prefs=await getPreferences(actor.id),rows=await listAdminPosts();return <main className="min-h-screen bg-slate-50 px-4 py-6"><div className="mx-auto max-w-7xl"><AdminNav locale={prefs.interfaceLanguage}/><div className="mt-8 flex items-center justify-between"><div><h1 className="text-3xl font-black">Bài viết SEO</h1><p className="mt-2 text-slate-600">Soạn, xem trước và xuất bản nội dung TOEIC.</p></div><Link className="rounded-xl bg-teal-700 px-5 py-3 font-black text-white" href="/admin/content/posts/new">Bài viết mới</Link></div><div className="mt-6 overflow-x-auto rounded-2xl border bg-white"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-slate-100"><tr>{["Title","Slug","Status","Category","Published","Updated"].map(x=><th className="p-4" key={x}>{x}</th>)}</tr></thead><tbody>{rows.map(x=><tr className="border-t" key={x.id}><td className="p-4"><Link className="font-bold text-teal-700" href={`/admin/content/posts/${x.id}`}>{x.title}</Link></td><td className="p-4 font-mono text-xs">{x.slug}</td><td className="p-4 font-bold">{x.status}</td><td className="p-4">{x.category}</td><td className="p-4">{x.publishedAt?.toLocaleDateString()??"—"}</td><td className="p-4">{x.updatedAt.toLocaleString()}</td></tr>)}</tbody></table>{!rows.length&&<p className="p-10 text-center text-slate-500">Chưa có bài viết.</p>}</div></div></main>}
