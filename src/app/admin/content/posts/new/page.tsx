import { AdminNav } from "@/components/admin/admin-nav";import { PostForm } from "@/components/admin/post-form";import { requireAdmin } from "@/lib/admin/authorization";import { listReadyCoverImages } from "@/lib/blog/service";import { getPreferences } from "@/lib/i18n/get-translations";
export default async function NewPost(){const actor=await requireAdmin("CONTENT_MANAGE"),prefs=await getPreferences(actor.id),images=await listReadyCoverImages();return <main className="min-h-screen bg-slate-50 px-4 py-6"><div className="mx-auto max-w-4xl"><AdminNav locale={prefs.interfaceLanguage}/><h1 className="mt-8 text-3xl font-black">Bài viết mới</h1><PostForm images={images} initial={{title:"",slug:"",excerpt:"",content:"",category:"TOEIC_STRATEGY",seoTitle:"",seoDescription:"",canonicalPath:"",coverMediaId:"",tags:""}}/></div></main>}
