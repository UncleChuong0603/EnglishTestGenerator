import Link from "next/link";import { notFound } from "next/navigation";import { AdminNav } from "@/components/admin/admin-nav";import { PostForm } from "@/components/admin/post-form";import { deletePostAction,publishPostAction,unpublishPostAction } from "../actions";import { requireAdmin } from "@/lib/admin/authorization";import { getPostById,listReadyCoverImages } from "@/lib/blog/service";import { getPreferences } from "@/lib/i18n/get-translations";import type { PostCategory } from "@/lib/blog/core";
export default async function EditPost({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){const actor=await requireAdmin("CONTENT_MANAGE"),prefs=await getPreferences(actor.id),{id}=await params,[post,images]=await Promise.all([getPostById(id),listReadyCoverImages()]),query=await searchParams;if(!post)notFound();const hidden=<><input name="id" type="hidden" value={post.id}/><input name="slug" type="hidden" value={post.slug}/></>;return <main className="min-h-screen bg-slate-50 px-4 py-6"><div className="mx-auto max-w-4xl"><AdminNav locale={prefs.interfaceLanguage}/><div className="mt-8 flex flex-wrap items-center justify-between gap-3"><div><h1 className="text-3xl font-black">Chỉnh sửa bài viết</h1><p className="mt-2 font-bold text-slate-500">{post.status}</p></div><div className="flex gap-2"><Link className="rounded-xl border bg-white px-4 py-3 font-bold" href={`/admin/content/posts/${id}/preview`}>Xem trước</Link>{post.status==="PUBLISHED"?<form action={unpublishPostAction}>{hidden}<button className="rounded-xl bg-amber-600 px-4 py-3 font-bold text-white">Gỡ xuất bản</button></form>:<form action={publishPostAction}>{hidden}<button className="rounded-xl bg-emerald-700 px-4 py-3 font-bold text-white">Xuất bản</button></form>}</div></div>{query.error&&<p className="mt-5 rounded-xl bg-red-50 p-4 font-bold text-red-800">Không thể hoàn tất: {query.error}</p>}<PostForm images={images} initial={{id:post.id,title:post.title,slug:post.slug,excerpt:post.excerpt,content:post.content,category:post.category as PostCategory,seoTitle:post.seoTitle??"",seoDescription:post.seoDescription??"",canonicalPath:post.canonicalPath??"",coverMediaId:post.coverMediaId??"",tags:post.tags.map(x=>x.name).join(", ")}}/>{post.status!=="PUBLISHED"&&<form action={deletePostAction} className="mt-8 border-t pt-6">{hidden}<button className="rounded-xl border border-red-300 px-4 py-3 font-bold text-red-700">Xóa bản nháp</button></form>}</div></main>}
