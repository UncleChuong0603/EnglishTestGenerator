import Link from "next/link";
import { AdminNav } from "@/components/admin/admin-nav";
import { requireAdmin } from "@/lib/admin/authorization";
import { listAdminContent } from "@/lib/admin/content";
import { getPreferences } from "@/lib/i18n/get-translations";

const filterKeys = ["domain","part","lifecycle","difficulty","skill","subSkill","provenance","batch"] as const;
export default async function Page({ searchParams }: { searchParams: Promise<Record<string,string|undefined>> }) {
  const q=await searchParams, actor=await requireAdmin("CONTENT_READ"), prefs=await getPreferences(actor.id);
  const d=await listAdminContent({search:q.search,domain:q.domain,part:q.part?Number(q.part):undefined,lifecycle:q.lifecycle,difficulty:q.difficulty,skill:q.skill,subSkill:q.subSkill,provenance:q.provenance,batch:q.batch,page:Number(q.page)||1});
  const context=new URLSearchParams(); filterKeys.forEach((key)=>{if(q[key])context.set(key,q[key]!)}); const exportParams=new URLSearchParams(context); if(q.part) exportParams.set("part",q.part);
  return <main className="min-h-screen bg-slate-50 px-4 py-6"><div className="mx-auto max-w-7xl"><AdminNav locale={prefs.interfaceLanguage}/>
    <div className="mt-8 flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-bold uppercase tracking-widest text-teal-700">Content operations</p><h1 className="text-3xl font-black">Ngân hàng câu hỏi</h1><p className="mt-1 text-slate-600">Lọc, review, import và xuất nội dung TOEIC.</p></div><div className="flex gap-2"><Link href="/admin/content/import" className="rounded-lg border bg-white px-4 py-2 font-bold">Import JSON</Link><Link href="/admin/content/new" className="rounded-lg bg-teal-700 px-4 py-2 font-bold text-white">Tạo Draft</Link></div></div>
    <form className="mt-5 grid gap-3 rounded-2xl border bg-white p-4 md:grid-cols-4"><input aria-label="Tìm kiếm" name="search" defaultValue={q.search} placeholder="Tìm nội dung hoặc ID" className="rounded-lg border p-2"/><select aria-label="Domain" name="domain" defaultValue={q.domain??""} className="rounded-lg border p-2"><option value="">Mọi domain</option><option>LISTENING</option><option>READING</option></select><select aria-label="Part" name="part" defaultValue={q.part??""} className="rounded-lg border p-2"><option value="">Mọi part</option>{[1,2,3,4,5,6,7].map(p=><option key={p}>{p}</option>)}</select><select aria-label="Trạng thái" name="lifecycle" defaultValue={q.lifecycle??""} className="rounded-lg border p-2"><option value="">Mọi trạng thái</option><option value="draft">Draft</option><option value="published">Published</option><option value="archived">Archived</option></select><input name="skill" defaultValue={q.skill} placeholder="Skill" className="rounded-lg border p-2"/><input name="subSkill" defaultValue={q.subSkill} placeholder="Subskill" className="rounded-lg border p-2"/><select name="difficulty" defaultValue={q.difficulty??""} className="rounded-lg border p-2"><option value="">Mọi độ khó</option><option>easy</option><option>medium</option><option>hard</option></select><input name="batch" defaultValue={q.batch} placeholder="Batch key" className="rounded-lg border p-2"/><select name="provenance" defaultValue={q.provenance??""} className="rounded-lg border p-2"><option value="">Mọi nguồn</option><option>SEEDED</option><option>ADMIN</option></select><button className="rounded-lg bg-slate-900 p-2 font-bold text-white">Áp dụng bộ lọc</button><Link href="/admin/content/questions" className="rounded-lg border p-2 text-center font-semibold">Xóa lọc</Link></form>
    <div className="mt-4 flex flex-wrap items-center justify-between gap-3 rounded-xl border border-teal-200 bg-teal-50 p-4"><p><strong>{d.totalQuestions}</strong> câu hỏi trong {d.total} nhóm khớp bộ lọc{q.part?` · Part ${q.part}`:""}</p>{q.part?<a href={`/api/admin/content-export?${exportParams}`} className="rounded-lg bg-teal-700 px-4 py-2 font-bold text-white">Export {d.totalQuestions} câu · JSON</a>:<span className="text-sm text-slate-600">Chọn Part để bật Export JSON.</span>}</div>
    <div className="mt-5 overflow-x-auto rounded-2xl border bg-white"><table className="w-full min-w-[900px] text-left"><thead><tr className="border-b bg-slate-100 text-sm uppercase tracking-wide text-slate-600"><th className="p-3">Nội dung</th><th>Domain / Part</th><th>Trạng thái</th><th>Nguồn / Batch</th><th>Số câu</th><th>Thao tác</th></tr></thead><tbody>{d.rows.map(r=>{const href=`/admin/content/questions/${r.id}${context.size?`?${context}`:""}`;return <tr key={r.id} className="border-b align-top hover:bg-slate-50"><td className="p-3"><Link className="font-bold text-teal-700" href={href}>{r.title}</Link><small className="block text-slate-500">Cập nhật {r.updatedAt.toLocaleString()}</small></td><td className="py-3">{r.domain} · P{r.part}<br/><span className="text-sm text-slate-500">{r.setType}</span></td><td className="py-3"><span className={`rounded-full px-2 py-1 text-xs font-bold ${r.lifecycle==="draft"?"bg-amber-100 text-amber-900":r.lifecycle==="published"?"bg-emerald-100 text-emerald-900":"bg-slate-200 text-slate-700"}`}>{r.lifecycle.toUpperCase()}</span></td><td className="py-3">{r.provenance}{r.batchKey&&<><br/><Link className="text-sm font-semibold text-teal-700 underline" href={`/admin/content/batches/${encodeURIComponent(r.batchKey)}`}>{r.batchKey}</Link></>}</td><td className="py-3">{r.questions}</td><td className="py-3"><Link className="font-bold text-teal-700" href={href}>Review →</Link></td></tr>})}</tbody></table>{!d.rows.length&&<p className="p-8 text-center text-slate-500">Không có nội dung phù hợp.</p>}</div><p className="mt-4">Trang {d.page} · {d.total} kết quả</p>
  </div></main>;
}
