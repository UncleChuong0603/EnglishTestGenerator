import Link from "next/link"; import { AdminNav } from "@/components/admin/admin-nav"; import { requireAdmin } from "@/lib/admin/authorization"; import { getContentOverview } from "@/lib/admin/content"; import { getPreferences } from "@/lib/i18n/get-translations";
export default async function Page(){const actor=await requireAdmin("CONTENT_READ"),prefs=await getPreferences(actor.id),d=await getContentOverview();const cards=[["P1",`${d.listening.p1} / 6`],["P2",`${d.listening.p2} / 25`],["P3",`${d.listening.p3Groups} nhóm · ${d.listening.p3Questions} câu`],["P4",`${d.listening.p4Groups} nhóm · ${d.listening.p4Questions} câu`],["P5",`${d.reading.p5} câu`],["P6",`${d.reading.p6Groups} nhóm · ${d.reading.p6Questions} câu`],["P7 single",`${d.reading.p7SingleGroups} nhóm · ${d.reading.p7SingleQuestions} câu`],["P7 multiple",`${d.reading.p7MultipleGroups} nhóm · ${d.reading.p7MultipleQuestions} câu`]];return <main className="min-h-screen bg-slate-50 px-4 py-6 text-slate-900"><div className="mx-auto max-w-6xl"><AdminNav locale={prefs.interfaceLanguage}/><div className="mt-8 flex flex-wrap items-end justify-between gap-3"><div><h1 className="text-3xl font-black">Nội dung</h1><p className="text-slate-600">Coverage và trạng thái ngân hàng câu hỏi.</p></div><div className="flex gap-2"><Link className="rounded-lg border bg-white px-4 py-2 font-bold" href="/admin/content/questions">Câu hỏi / Nhóm</Link><Link className="rounded-lg border bg-white px-4 py-2 font-bold" href="/admin/content/media">Media</Link><Link className="rounded-lg bg-teal-700 px-4 py-2 font-bold text-white" href="/admin/content/new">Tạo Draft</Link></div></div><section className={`mt-6 rounded-2xl border p-6 ${d.ready?"border-emerald-300 bg-emerald-50":"border-red-300 bg-red-50"}`}><h2 className="text-xl font-black">Full Mock Readiness: {d.ready?"READY":"NOT READY"}</h2><p>P7 exact feasibility: single 10/29 {d.ready?"YES":"NO"} · multiple 5/25 {d.ready?"YES":"NO"}</p></section><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([a,b])=><article className="rounded-2xl border bg-white p-5" key={a}><p className="font-bold text-slate-500">{a}</p><p className="mt-2 text-xl font-black">{b}</p></article>)}</div><section className="mt-6 rounded-2xl border bg-white p-5"><h2 className="font-black">Lifecycle</h2><div className="mt-3 flex gap-6"><span>Draft: {d.lifecycle.draft??0}</span><span>Published: {d.lifecycle.published??0}</span><span>Archived: {d.lifecycle.archived??0}</span></div></section></div></main>}
