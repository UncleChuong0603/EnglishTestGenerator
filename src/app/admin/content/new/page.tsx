import { eq } from "drizzle-orm"; import { db } from "@/db"; import { mediaAssets } from "@/db/schema"; import { AdminNav } from "@/components/admin/admin-nav"; import { ContentForm } from "@/components/admin/content-form"; import { requireAdmin } from "@/lib/admin/authorization"; import { getPreferences } from "@/lib/i18n/get-translations";
export default async function Page(){const actor=await requireAdmin("CONTENT_MANAGE"),prefs=await getPreferences(actor.id),media=await db.select({id:mediaAssets.id,kind:mediaAssets.kind}).from(mediaAssets).where(eq(mediaAssets.status,"READY")).limit(100);return <main className="min-h-screen bg-slate-50 px-4 py-6"><div className="mx-auto max-w-5xl"><AdminNav locale={prefs.interfaceLanguage}/><h1 className="my-8 text-3xl font-black">Tạo nội dung Draft</h1><ContentForm media={media}/></div></main>}
