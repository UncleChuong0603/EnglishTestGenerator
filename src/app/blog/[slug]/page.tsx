import type { Metadata } from "next";import { notFound } from "next/navigation";import { PublicHeader } from "@/components/public-header";import { PublicFooter } from "@/components/public-footer";import { ArticleView } from "@/components/blog/article-view";import { getPublishedPost,coverUrl } from "@/lib/blog/service";import { getCurrentUser } from "@/lib/auth/session";import { getPreferences } from "@/lib/i18n/get-translations";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params,post=await getPublishedPost(slug);if(!post)return {title:"Không tìm thấy bài viết",robots:{index:false,follow:false}};const image=await coverUrl(post.coverMediaId),canonical=post.canonicalPath||`/blog/${post.slug}`,title=post.seoTitle||post.title,description=post.seoDescription||post.excerpt;return {title,description,alternates:{canonical},openGraph:{title,description,type:"article",publishedTime:post.publishedAt?.toISOString(),modifiedTime:post.updatedAt.toISOString(),url:canonical,images:image?[image]:[]}};}
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params,post=await getPublishedPost(slug);if(!post)notFound();const user=await getCurrentUser(),prefs=await getPreferences(user?.id),image=await coverUrl(post.coverMediaId),base=process.env.APP_URL??"http://localhost:3000",url=new URL(post.canonicalPath||`/blog/${post.slug}`,base).toString();const jsonLd={"@context":"https://schema.org","@type":"BlogPosting",headline:post.title,description:post.seoDescription||post.excerpt,datePublished:post.publishedAt?.toISOString(),dateModified:post.updatedAt.toISOString(),url,image:image||undefined,author:{"@type":"Organization",name:"TOEICGym"},publisher:{"@type":"Organization",name:"TOEICGym"}};return <main className="min-h-screen bg-white"><PublicHeader locale={prefs.interfaceLanguage} signedIn={Boolean(user)}/><script dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd).replace(/</g,"\\u003c")}} type="application/ld+json"/><ArticleView coverUrl={image} locale={prefs.interfaceLanguage} post={post}/><PublicFooter locale={prefs.interfaceLanguage}/></main>}
