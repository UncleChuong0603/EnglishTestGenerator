import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync("drizzle/0012_ranking_challenges.sql","utf8"),runtime=readFileSync("drizzle/0013_ranked_challenge_runtime.sql","utf8"),journal=readFileSync("drizzle/meta/_journal.json","utf8");
describe("Task 16 migrations",()=>{it("appends 0012 and 0013 after protected 0011",()=>{const j=JSON.parse(journal);const tags=j.entries.map((x:{tag:string})=>x.tag);expect(tags.slice(-3)).toEqual(["0011_practice_session_count_invariants","0012_ranking_challenges","0013_ranked_challenge_runtime"])});it("has privacy, idempotency and one-attempt constraints",()=>{expect(sql).toContain("DEFAULT 'ANONYMOUS'");expect(sql).toContain("UNIQUE NULLS NOT DISTINCT");expect(sql).toContain('CONSTRAINT "ranked_challenge_runs_user_unique" UNIQUE')});it("adds runtime deadlines, child linkage and archive guard",()=>{expect(runtime).toContain('"listening_deadline"');expect(runtime).toContain('"ranked_challenge_run_id"');expect(runtime).toContain("protect_active_challenge_content")});it("does not backfill history",()=>{expect((sql+runtime).toLowerCase()).not.toMatch(/insert into "gamification_events"\s+select/)})});
