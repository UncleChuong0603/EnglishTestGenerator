const provider=process.env.PAYMENT_PROVIDER??"PAYOS",environment=process.env.NODE_ENV??"development";
const configured=(name)=>Boolean(process.env[name]);const price=(name)=>{const raw=process.env[name];return raw&&/^\d+$/.test(raw)&&Number.isSafeInteger(Number(raw))&&Number(raw)>0?Number(raw):null};
const credentials={clientId:configured("PAYOS_CLIENT_ID"),apiKey:configured("PAYOS_API_KEY"),checksumKey:configured("PAYOS_CHECKSUM_KEY")};const prices={days30:price("PREMIUM_30_PRICE_VND"),days90:price("PREMIUM_90_PRICE_VND"),days365:price("PREMIUM_365_PRICE_VND")};
let origin="INVALID";try{const url=new URL(process.env.APP_URL??"");origin=url.origin;if(environment==="production"&&(url.protocol!=="https:"||url.hostname!=="toeicgym.net"))origin="INVALID"}catch{}
const fakeAllowed=provider==="FAKE"&&environment!=="production";const providerReady=provider==="PAYOS"&&Object.values(credentials).every(Boolean);const checkout=providerReady&&Object.values(prices).every(value=>value!==null)&&origin!=="INVALID";
const state=(value)=>value?"CONFIGURED":"NOT CONFIGURED",priceState=(value)=>value===null?"NOT CONFIGURED":`CONFIGURED (${new Intl.NumberFormat("vi-VN").format(value)} VND)`;
console.log(`Environment: ${environment}`);console.log(`Provider: ${provider}`);console.log(`Client ID: ${state(credentials.clientId)}`);console.log(`API Key: ${state(credentials.apiKey)}`);console.log(`Checksum Key: ${state(credentials.checksumKey)}`);console.log(`30-day price: ${priceState(prices.days30)}`);console.log(`90-day price: ${priceState(prices.days90)}`);console.log(`365-day price: ${priceState(prices.days365)}`);console.log(`Application origin: ${origin}`);console.log(`Webhook URL: ${origin==="INVALID"?"INVALID":`${origin}/api/payments/payos/webhook`}`);console.log(`Return URL: ${origin==="INVALID"?"INVALID":`${origin}/billing/return`}`);console.log(`Cancel URL: ${origin==="INVALID"?"INVALID":`${origin}/billing/cancel`}`);console.log(`Checkout: ${checkout?"ENABLED":"DISABLED"}`);console.log(`Fake provider: ${fakeAllowed?"ALLOWED":"BLOCKED"}`);
